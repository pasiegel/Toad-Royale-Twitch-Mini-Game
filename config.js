// Toad Royale configuration - edit this file to point the game at YOUR Twitch channel.
// (A plain .json file can't be loaded here: OBS/browsers open index.html via a
// file:// URL, and browsers block fetch() of local files for security. A tiny
// script that assigns one object is the file://-safe equivalent - edit the
// value in quotes below just like you would a JSON field.)
const GAME_CONFIG = {
  channel: "TwitchChannelName"
};
